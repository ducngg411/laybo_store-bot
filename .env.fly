# Fly.io Database Connection (via proxy)
# Run: fly proxy 5433:5432 -a laybo-store-db first
# Then use this DATABASE_URL to connect

DATABASE_URL=postgres://laybo_store_bot:N16PZBBxa0XHW4q@localhost:5433/laybo_store_bot?sslmode=disable
